import { Container } from "react-bootstrap";

const CartItem =({id, title, image, qty, price}) => {
    return (
        <Container>
            
        </Container>
    )
}

export default CartItem;